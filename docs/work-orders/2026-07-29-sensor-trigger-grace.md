# 工作单：传感器延迟触发误报修复

- 目标：修复上方传感器在送料理论时间结束后稍晚触发却被判定为未触发的问题，并纠正 Fluidd 将驱动业务错误显示为 API 断联的问题。
- 范围：`extras/ace.py`、根 `ace.cfg`、配置规范、Fluidd API/错误展示、自动测试、生产构建和项目记忆。
- 禁止项：不增加送料距离或硬上限，不取消消抖，不自动执行送料、回抽、切刀、加热或换料，不提交或推送 GitHub。
- 验收：延迟触发在有限宽限期内成功停止；超期仍失败并带诊断；业务错误不显示为断网；Python、Web、Fluidd 类型检查、lint 和构建通过。
- 回滚：部署前后使用项目打印机备份工具归档；失败时恢复变更前驱动、配置和 Fluidd 构建。
