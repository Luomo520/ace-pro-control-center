# 更新日志

## 未发布

- 正式实现 `ace_config_version` 配置兼容标识；未填写新键的旧 `ace.cfg` 继续按
  兼容模式加载，升级默认保留原配置，不强制替换为新模板。
- 正式实现上方和下方传感器独立消抖：
  `extruder_sensor_debounce_count` 与 `toolhead_sensor_debounce_count` 分别约束送料
  停止和工具头到达判定，不复用五通或断料消抖。
- 正式实现 `toolchange_feed_hard_limit` 与 `toolchange_retract_hard_limit`，为正常
  路径、打滑补偿、回料和相关恢复建立绝对边界；达到硬上限后停止换料、记录
  失败阶段并暂停正在打印的任务，不取消打印。
- 自动重连恢复会继承同一次换料已经消耗的送料与回料预算，不会因重连重新获得
  完整上限。
- 自动探测料管长度和冷态预装载使用独立累计预算，所有 ACE 物理请求都在发送前
  检查硬上限。
- 修正五通传感器位于五通前时的回料最低限额计算，送料方向的停放偏移不再重复
  计入回料方向。
- 本轮验证范围限定为配置解析、服务/API/状态等静态部署验证；未执行送料、回抽、
  切刀、自动探测或完整换料的真机物理动作验证。
- 自动探测速度和分段距离改为 `[ace]` 可配置项：共用默认速度 `25 mm/s`，常规和末段默认 `50/50 mm`。
- 保留可选 `calibration_feed_speed` 与 `calibration_retract_speed`，需要时可分别覆盖送料和回抽速度。
- 删除每个探测分段完成后的重复 ACE 就绪等待；典型 1200 至 1500 mm 路径由最多约 240 至 300 个测量段降至约 24 至 30 个。
- 保留传感器实时停止、最大距离、动作互斥、最终传感器检查和 USB 断联后不重放物理动作等安全保护；默认粗测结果按约 `±50 mm` 精度使用。
- 新增可配置粗测、独立速度覆盖、无逐段重复等待和配置默认值回归测试，并补充驱动手册、配置样式、项目决策与项目记忆。
- 将自动探测速度、分段和回抽上限合并到 `ace.cfg` 的“五通传感器与自动探测料管长度”区域，新增支持五通前/后传感器的完整字符路径图，以及每个条目的用途、单位、填写方法和覆盖关系。
- 修复送料失败结束后底部仍长期显示活动故障：Moonraker 仅在换料仍活动或等待恢复时发布警告，历史错误继续保留在诊断字段中。
- 无活动机械动作时，“清除故障”可复位换料和探测的历史软件错误，不修改传感器、槽位或耗材位置；活动动作中仍执行真实停止流程。
- Fluidd 与备用页面统一机械操作锁，覆盖状态缓存过期、打印/暂停、换料恢复、动作锁、无限续料和 ACE busy；自动探测按钮会明确显示具体传感器不可用或仍有料原因。
- 备用页面的完整轮询响应现在替换关键动作锁和传感器状态，避免旧缓存被继续当作实时状态；Fluidd `v1.37.2` 生产构建已同步。
- 新增 `ACE_CONFIG_SPECIFICATION.zh-CN.md` 和配置功能区编写模板，统一功能总览、安装/动作/条件五星标记、参数职责和变更门槛；根目录 `ace.cfg` 是唯一可安装模板，活动参数必须由驱动读取并由测试覆盖。
- 重排 `ace.cfg` 功能区并补齐逐项用途、单位、依赖、风险和代码回退值说明；先保留原有 76 个活动参数，再加入 5 个已实现的配置契约参数，配置布局测试锁定模板、材料分区和未来变更同步规则。

## [1.2.0] - 2026-07-28

- 项目正式更名为 **Ace Pro Control Center / ACE Pro 管理中心**，安装目录改为 `~/ace-pro-control-center`，状态与回滚目录改为 `~/.local/share/ace-pro-control-center`。
- 明确本项目是从 `szkrisz/ACEPROSV08` 衍生的独立 GPL-3.0 项目；保留 `[ace]`、`ACE_*`、`/server/ace/*` 和旧驱动身份作为运行时兼容接口。
- 重写中英文字符安装器，支持完整安装、仅驱动、仅卡片、强制安装、最近版本回滚、首次安装基线恢复、卸载和状态查询。
- 安装器不扫描、不判断、不迁移或删除 Kobra-S1、ACEPROSV08 等其他驱动；按全新安装处理，并在配置向导中询问上下传感器和可选五通传感器。
- 修复安装后 `ace.cfg` 因指向配置目录外部而在 Fluidd 显示锁图标的问题；新安装使用配置目录内的普通可写文件，旧软链接更新时先归档并保留实际内容后再无损迁移。
- 任何实际写入前先建立分范围 `old/` 归档；安装失败会恢复原文件，回滚和卸载前也会再次归档当前版本。
- Fluidd 或 Moonraker 版本超出完整测试范围时，可选择仅安装驱动、继续完整安装或取消；强制模式也不会跳过文件校验、归档和失败恢复。
- 配置的 `serial` 现在真正生效；仅当填写 `auto` 或 `detect` 时才扫描串口。上下传感器引脚缺失时给出明确启动错误，不再猜测 MCU 引脚。
- 新增 `enable_debug_rpc: False` 安全默认值，任意 ACE RPC 调试入口默认关闭。
- 默认 `_ACE_PRE_TOOLCHANGE`、`_ACE_POST_TOOLCHANGE` 不再移动、归零、加热或清洁喷嘴；`CUT_TIP` 保持为必须按机器填写的注释示例。
- 送料或回抽请求在 USB 断联后状态不确定时立即停止后续分段，并进入有界的传感器协调恢复，不会盲目重放物理动作。
- 未记录当前槽位但上下传感器检测到耗材时拒绝盲目卸载；无限续料完成后同步当前槽位、耗材位置和四槽位置状态。
- `ACE_SET_SLOT` 在修改库存前严格校验 RGB 为 `0-255`，无效输入不会污染内存或保存变量。
- 自动烘干连续失败达到上限后进入长退避，随后允许重新开始一轮有界尝试，避免永久停止恢复。
- Fluidd 与 Moonraker 同时识别新身份 `ACE_PRO_CONTROL_CENTER` 和兼容身份 `ACEPROSV08`，并显示换料恢复状态。
- 删除配置模板内重复的 `[save_variables]` 与 `[respond]` 全局节；用户现有 Klipper 全局配置保持独立，避免重复 section 导致启动失败。
- 完整安装现在同时建立驱动与卡片恢复基线，后续可单独卸载驱动或卡片；分范围卸载不会破坏另一组件，完整卸载仍可恢复项目首次写入前状态。
- 普通非交互安装遇到未知或不匹配的 Fluidd/Moonraker 版本时默认停止；只有显式 `--install-force` 才能越过兼容性阻断，且仍保留校验、归档和失败恢复。
- 安装、回滚和卸载拒绝以 `root` 身份运行，危险的直接 CLI 操作默认要求确认；`--yes` 只跳过确认，不再等同于强制兼容安装。
- 新增 Ubuntu GitHub Actions 发布验证：覆盖 Python、Web、Shell、五组安装事务、真实 POSIX 软链接，以及固定 Fluidd `v1.37.2` 的测试、类型检查、lint、构建和产物逐文件比较。
- 重写 README、完整安装恢复教程、驱动调校手册、功能清单、开发手册、项目记忆、决策记录和发布说明，使全新安装、升级、回滚、卸载与排障不再依赖聊天历史。
- 规范 GPL-3.0 许可证正文和上游署名，补充 Vue MIT 许可证；发布默认状态不再携带开发机器的真实槽位、材料或颜色数据。

## [1.1.0] - 2026-07-27

- 新增自动跟随打印烘干：连续两次检测到打印后启动，暂停时保持，完成、取消、错误或待机后停止自动拥有的任务。
- 按全部 PLA 45°C、PLA 与其他材料混装 50°C、未知材料 45°C、高温材料 60°C 计算温度，并服从 `max_dryer_temperature` 上限。
- 打印中材料变化只允许自动降温；所有槽位变为空时停止自动烘干，手动启动的烘干不会被自动停止。
- 修复打印结束与异步启动响应竞态，避免打印结束后才开始烘干。
- 修复 USB 断联导致启动或停止请求永久卡在待处理状态的问题；丢失请求会在重连后按 30 秒退避重试。
- 启动和停止失败最多重试三次，避免每秒重复发送命令；停止失败前保留自动任务所有权和真实运行状态。
- 修复 Klipper 重启后通知编号归零，Fluidd 不再显示后续自动烘干警告的问题。
- Fluidd 卡片与 `/ace.html` 增加自动跟随开关、当前状态、温度依据和材料安全提示。
- 修复快捷操作区“无限续料”开关错位，并保持窄卡片和移动端自然换行。
- 新增送料与回料距离标定，按上方传感器触发范围、`bowden_tube_length` 和安全余量计算五通估算停放距离，确认后才持久化。
- 新增四槽独立耗材位置状态、冷态预装载、完整卸载和紧急停止；普通 T0-T3 仍始终完成喷嘴装载。
- Fluidd 卡片与 `/ace.html` 对齐标定状态、传感器、送料/回料结果和全部受控命令，移除后端已不支持的人工 ACK。
- 手动送料、手动回抽、预装载、标定和完整卸载均要求当次确认并发送 `CONFIRM=1`，避免复用旧确认状态。
- 补充旧版位置状态迁移、标定过期回退、Fluidd 版本风险和安装前归档回滚说明。

## v1.0.0 - 2026-07-25

- 首个稳定版本：统一发布增强版 ACEPROSV08 驱动、Moonraker 受控 API、Fluidd 原生卡片、中文辅助页面和事务式安装器。
- 重写项目首页和中文教程，明确上游来源、GPL-3.0 许可、适用范围、安装菜单、必填配置、验证、回滚与故障排查。
- 加入 Fluidd 卡片局部视图和完整仪表盘两张实机截图。
- 连续送料默认取消固定距离停顿，上方传感器触发后按动作时长动态等待 ACE 恢复 `ready`。
- 自动回抽默认采用一个快速段和一个慢速停放段，取消每 100 mm 停顿；保留断续兼容开关。
- 延续安装前 `old/` 归档、失败自动恢复、最近版本回滚、首次安装基线恢复和 Fluidd 版本风险提示。

## v0.3.2 - 2026-07-25

- 新增 `ace_stop_ready_timeout`，传感器触发并停止送料后按“请求距离/速度 + 3 秒”动态等待 ACE 恢复 `ready`，修复 `400 mm / 25 mm/s` 补偿动作被 15 秒普通超时提前中断的问题。
- 新增 `intermittent_retract` 回抽模式开关，默认 `False` 将自动长距离回抽合并为一个快速段和一个慢速停放段。
- 设置 `intermittent_retract: True` 可恢复按 `ace_motion_chunk_length` 逐段回抽的兼容行为。
- 状态接口新增停止等待时间和回抽模式，便于 Fluidd 与日志诊断当前配置。

## v0.3.1 - 2026-07-25

- 新增 `intermittent_feed` 断续送料开关，默认 `False` 使用连续送料。
- 连续模式下，ACE 主路径只发送一条完整距离请求，并在请求执行期间实时监测上方传感器停止送料。
- 连续主送料结束仍未触发传感器时，只执行一次完整的有限低速打滑补偿，不再每 50/100 mm 停顿。
- 设置 `intermittent_feed: True` 可恢复原有快速分段、末段慢速接近和逐段补偿行为。
- 连续送料或补偿期间若 USB 状态不确定，驱动不会重复发送物理动作，换料按原安全策略暂停。

## v0.3.0 - 2026-07-20

- 将增强版 ACEPROSV08 驱动、配置模板、Moonraker API、Fluidd 卡片和中文独立页面合并为一个安装包。
- 安装器改为完整事务：安装、更新和卸载前把旧文件移动到 `old/<时间戳>/old/`，失败时自动恢复，新文件不直接删除。
- 默认菜单项安装驱动、配置、卡片和 Moonraker 适配层；新增仅驱动、仅卡片和对应范围卸载选项，范围互不覆盖。
- 安装前比较当前 Fluidd 与测试版本 `v1.37.2`；低版本、高版本和未知版本均提示兼容风险，由用户决定继续或取消。
- 新增最近一次安装回滚，当前版本同样先进入 `old/`，与恢复首次安装前状态的完整卸载互相独立。
- 默认更新保留现有 `ace.cfg`，新版模板保存为 `~/ACEPROSV08/ace.cfg.example`；新增显式替换模板选项。
- 自动建立 Klipper 驱动与 ACE 配置软链接，并补充 `[include ace.cfg]`、`[ace_status]` 和 pyserial 依赖检查。
- 长距离送料采用快速段、最后 200 mm 慢速接近和最多 400 mm 有限打滑补偿，快速阶段取消每 100 mm 停顿。
- 回收采用 120 mm/s 快速段与最后 200 mm 慢速停放；工具头送料按 5 mm/1 mm 分段检查下方传感器。
- 送料失败改为暂停打印，并报告换料方向、失败阶段、移动距离和传感器状态。
- 增加连接代次、断联原因、换料上下文和有界自动恢复状态，避免不受控的物理动作重放。
- 控制台换料、连接、烘干、库存、无限续料和诊断提示统一为中文，`ACE_QUERY_SLOTS` 输出中文四槽列表。
- `ace.cfg` 顶部增加耗材路径图、安装前必填参数和全部速度/距离参数的中文说明。
- Moonraker 状态新增驱动版本、连接和换料恢复信息；Fluidd 卡片增加对应诊断和终止恢复能力。
- 使用当前卡片源码重新构建 Fluidd v1.37.2 生产产物，避免源码与发布目录不一致导致卡片缺失或空白页面。
- 新增一体化安装教程和驱动调校文档，补充升级、回滚、首次动作测试和常见故障说明。

## v0.2.0 - 2026-07-15

- 修复安装或恢复 Fluidd 后由于目录权限过严，Nginx 无法读取 JavaScript，页面显示空白的问题。
- 安装完成和卸载恢复都会自动设置 Fluidd 文件为 Web 服务可读权限。
- 修复 ACEPROSV08 烘干状态读取，并兼容 `dryer`、`dryer_status`、`state` 和 `target_temperature` 字段别名。
- 烘干剩余秒数自动转换为分钟，手动修改的烘干温度不会被状态轮询覆盖。
- 增加 `ABSCF`、`PAHTCF`、`PETCF` 和 `PEEK` 耗材类型，并按材料设置默认烘干温度。
- 更新发布校验范围，避免用户修改未被安装器写入的驱动配置后无法更新面板。

## v0.1.0 - 2026-07-15

- 首次发布 ACEPROSV08 专用 Fluidd 卡片与中文独立页面迁移版。
- 新增 Moonraker `ace_status` 适配层，提供状态、料槽、能力和命令四个 API。
- 命令执行改为严格白名单，`ACE_SET_SLOT` 使用 ACEPROSV08 的 `INDEX` 参数。
- 新增字符菜单安装器，支持安装/更新、强制安装、卸载恢复和状态检查。
- 安装和卸载前均创建备份，备份目录为 `~/.local/share/aceprosv08-ui/backups/`。
- Fluidd 卡片和独立页面迁移自旧 ACE Pro UI，并补齐中文说明和第三方来源声明。
- 修复 Moonraker POST 请求读取方式，命令端点使用 `WebRequest.get_args()`。
- 修复 API 失败被 Fluidd 误判为成功，以及颜色/材料保存后恢复旧值的问题。
- 增加上下传感器状态、手动送料/回抽、助推、换卷、无限续料和诊断控制。
- RGB 参数改为严格校验，不再静默钳制越界值。
- 安装器记录文件安装前是否存在，卸载和失败回滚可恢复完整初始状态。
- 从 Fluidd v1.37.2 完整源码重新构建，并通过前端、Python 和隔离安装器测试。
